# Generated by tools/gen-tool-index.py. Do not edit.
# Same data as TOOLS.md, for anything that would rather not parse Markdown.
count: 50
tools:
  - name: "abi-gap.sh"
    path: "tools/abi-gap.sh"
    summary: "list the symbols a prebuilt blob imports that the running platform no longer provides"
    usage: "abi-gap.sh <blob> [-s SERIAL] [--keep DIR]"
    docs:
      - "docs/debugging-a-vendor-blob.md"
  - name: "apply-overlay.sh"
    path: "tools/apply-overlay.sh"
    summary: "apply the composed customization stack onto a synced LineageOS tree:"
    usage: "apply-overlay.sh"
    docs: []
  - name: "bench-launch.sh"
    path: "tools/bench-launch.sh"
    summary: "cold app-launch times over adb, for A/B-ing a runtime tuning on one phone"
    usage: "bench-launch.sh"
    docs: []
  - name: "blob-attach.sh"
    path: "tools/blob-attach.sh"
    summary: "start a vendor binary under lldb-server and print the load base of one of its libraries, so you can set absolute breakpoints inside a stripped prebuilt"
    usage: "blob-attach.sh <device-binary> <library-soname> [--port N] [-s SERIAL]"
    docs:
      - "docs/debugging-a-vendor-blob.md"
  - name: "boot-window-logcat.sh"
    path: "tools/boot-window-logcat.sh"
    summary: "catch the logcat of a boot that ends in a reboot, through the seconds adbd is reachable"
    usage: "boot-window-logcat.sh <outdir> [-s SERIAL] [--timeout SECONDS]        default timeout 900"
    docs: []
  - name: "check-bpf-objects.py"
    path: "tools/check-bpf-objects.py"
    summary: "which BPF programs and maps of a built Android tree this kernel cannot load"
    usage: "check-bpf-objects.py --kernel <kernel-src>/include/uapi/linux/bpf.h --kver 4.9.337 \\"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "check-bpf-readiness.sh"
    path: "tools/check-bpf-readiness.sh"
    summary: "find what a kernel without eBPF (or other modern syscalls) will break, before you spend a build-flash-boot cycle finding out"
    usage: "check-bpf-readiness.sh"
    docs: []
  - name: "check-hal-readiness.sh"
    path: "tools/check-hal-readiness.sh"
    summary: "find HAL problems BEFORE the build-flash-boot cycle"
    usage: "check-hal-readiness.sh"
    docs:
      - "docs/debugging-a-boot-loop.md"
  - name: "check-image-labels.sh"
    path: "tools/check-image-labels.sh"
    summary: "find paths in the image that no file_contexts entry labels"
    usage: "check-image-labels.sh"
    docs: []
  - name: "check-patch-series.sh"
    path: "tools/check-patch-series.sh"
    summary: "find patches that undo earlier patches in the same series"
    usage: "check-patch-series.sh [DEVICE_REPO] [--quiet]"
    docs: []
  - name: "check-platform-support.sh"
    path: "tools/check-platform-support.sh"
    summary: "find the upstream makefile gates that SILENTLY EXCLUDE this device's SoC"
    usage: "check-platform-support.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "check-sigpipe.sh"
    path: "tools/check-sigpipe.sh"
    summary: "fail if any shell script that runs under `set -o pipefail` pipes into `grep -q` or `head`"
    usage: "check-sigpipe.sh"
    docs: []
  - name: "check-tree-dirt.sh"
    path: "tools/check-tree-dirt.sh"
    summary: "every uncommitted change in a patched project should have been put there by forge"
    usage: "check-tree-dirt.sh [AOSP_ROOT]"
    docs: []
  - name: "dev-shell.sh"
    path: "tools/dev-shell.sh"
    summary: "authoring container. Host needs only Docker. Runs the build image with the repos root mounted at /repos: clone/edit/format-patch/git-am/commit in-container. Only `git push` uses host auth (GH_TOKEN, else `gh auth token`). dev-shell.sh [cmd...]   # no args = interactive shell Env: REPOS (default: parent of this repo), IMAGE (default aosp-los22:24.04), GH_TOKEN"
    usage: "dev-shell.sh [cmd...]   # no args = interactive shell"
    docs: []
  - name: "diag-efs.sh"
    path: "tools/diag-efs.sh"
    summary: "read a modem EFS/NV item file over /dev/diag, on a connected device"
    usage: "diag-efs.sh <buildid|hello|ls DIR|read PATH|write PATH HEX [OFLAG MODE]|rm PATH|probe HEX>"
    docs: []
  - name: "dtbo-ramoops-alt.py"
    path: "tools/dtbo-ramoops-alt.py"
    summary: "make the live ramoops console survive a clean reboot"
    usage: "dtbo-ramoops-alt.py <dtbo.img> <out.img> [--index N]"
    docs:
      - "docs/debugging-a-boot-loop.md"
      - "docs/porting-a-branch-bump.md"
  - name: "extract-gapps-apps.sh"
    path: "tools/extract-gapps-apps.sh"
    summary: "pull the Google versions of the stock apps out of a GApps zip and stage them as /product prebuilts, so a WITH_GAPPS build swaps the Lineage/AOSP apps for Google's"
    usage: "extract-gapps-apps.sh"
    docs: []
  - name: "extract-nextbit-oem-assets.sh"
    path: "tools/extract-nextbit-oem-assets.sh"
    summary: "reclaim the Nextbit Robin OEM assets (system sounds, wallpapers, boot animation) from a Nextbit Robin stock ROM and bake them into the tree being built"
    usage: "extract-nextbit-oem-assets.sh"
    docs:
      - "docs/OEM-ASSETS.md"
  - name: "fb-capture.sh"
    path: "tools/fb-capture.sh"
    summary: "screenshot a device that has no screencap:"
    usage: "fb-capture.sh [-o out.png] [-s SERIAL] [--fb N]"
    docs: []
  - name: "find-orphaned-sepolicy-types.sh"
    path: "tools/find-orphaned-sepolicy-types.sh"
    summary: "list SELinux types a device tree still references but that no longer exist, after an upstream branch bump deleted the policy that defined them"
    usage: "find-orphaned-sepolicy-types.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "find-removed-platform-symbols.sh"
    path: "tools/find-removed-platform-symbols.sh"
    summary: "C/C++ constants a device tree uses that the new branch deleted"
    usage: "find-removed-platform-symbols.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "find-soong-namespace-drift.sh"
    path: "tools/find-soong-namespace-drift.sh"
    summary: "Soong namespace and manifest changes between branches that will break a device tree the new branch no longer maintains"
    usage: "find-soong-namespace-drift.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "gen-tool-index.py"
    path: "tools/gen-tool-index.py"
    summary: "regenerate the tool index from the tools themselves"
    usage: "gen-tool-index.py            # rewrite docs/TOOLS.md and docs/tools.yaml"
    docs: []
  - name: "hybrid-bootimg.sh"
    path: "tools/hybrid-bootimg.sh"
    summary: "a boot image with one build's kernel+dtb and another's ramdisk"
    usage: "hybrid-bootimg.sh <kernel-from.img> <ramdisk-from.img> <out.img>"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "init-harness.sh"
    path: "tools/init-harness.sh"
    summary: "run a new ramdisk's /init on the live kernel, from recovery, without a boot"
    usage: "init-harness.sh <boot.img | ramdisk-dir> [-t SECONDS] [-o OUTDIR] [-c 'CMD ...'] [-s SERIAL]"
    docs:
      - "docs/debugging-a-boot-loop.md"
      - "docs/porting-a-branch-bump.md"
  - name: "kernel-rebuild.sh"
    path: "tools/kernel-rebuild.sh"
    summary: "rebuild just the boot image (or any make target) after a kernel change, with the environment of the last full build"
    usage: "kernel-rebuild.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "make-apex-key.sh"
    path: "tools/make-apex-key.sh"
    summary: "create the signing key an EROFS-repacked APEX is re-signed with"
    usage: "make-apex-key.sh <apex-name> <keys-dir>"
    docs: []
  - name: "make-keys.sh"
    path: "tools/make-keys.sh"
    summary: "generate the signing keys for release builds, once, into a directory outside every repo"
    usage: "make-keys.sh"
    docs:
      - "docs/RELEASING.md"
  - name: "mcfg-items.py"
    path: "tools/mcfg-items.py"
    summary: "list the EFS items a Qualcomm MCFG provisions, with their values"
    usage: "mcfg-items.py mcfg_sw.mbn              # every EFS item: path, size, value"
    docs: []
  - name: "measure-touch-rate.sh"
    path: "tools/measure-touch-rate.sh"
    summary: "Measure how fast the touchscreen actually reports, while a finger is down"
    usage: "measure-touch-rate.sh"
    docs: []
  - name: "new-device-repo.sh"
    path: "tools/new-device-repo.sh"
    summary: "scaffold a device repo from scratch"
    usage: "new-device-repo.sh"
    docs: []
  - name: "ota-extract.sh"
    path: "tools/ota-extract.sh"
    summary: "pull the partition images out of a signed A/B OTA zip, and optionally flash them to one slot"
    usage: "ota-extract.sh <ota.zip> <outdir> [--flash a|b] [--os-only] [-s SERIAL]"
    docs:
      - "docs/debugging-a-vendor-blob.md"
  - name: "pixel-ramoops-pull.sh"
    path: "tools/pixel-ramoops-pull.sh"
    summary: "decrypt the panic log a Pixel's bootloader saved, from recovery"
    usage: "pixel-ramoops-pull.sh --genkey <keydir>          once: make a keypair, load its pubkey"
    docs: []
  - name: "pmsg-decode.py"
    path: "tools/pmsg-decode.py"
    summary: "decode a pstore pmsg record into readable logcat text"
    usage: "pmsg-decode.py <pmsg-ramoops-N> [out.txt]"
    docs: []
  - name: "prop-denials.sh"
    path: "tools/prop-denials.sh"
    summary: "turn \"N domains are denied a property type\" into \"these domains want these specific properties\", so you can label the prefixes instead of widening the type"
    usage: "prop-denials.sh [--image DIR] [-s SERIAL] [--type TYPE]"
    docs:
      - "docs/debugging-a-vendor-blob.md"
  - name: "prop-effect.sh"
    path: "tools/prop-effect.sh"
    summary: "answer \"I set this property and nothing happened\" properly, instead of setting more properties"
    usage: "prop-effect.sh <property> [--image DIR] [-s SERIAL]"
    docs:
      - "docs/debugging-a-vendor-blob.md"
  - name: "propagate-forge.sh"
    path: "tools/propagate-forge.sh"
    summary: "push an engine change out to every device repo, now"
    usage: "propagate-forge.sh"
    docs: []
  - name: "pstore-pull.sh"
    path: "tools/pstore-pull.sh"
    summary: "pull every pstore record from a phone in recovery, plus (optionally) the raw ramoops region behind it"
    usage: "pstore-pull.sh <outdir> [--raw /dev/access-ramoops] [-s SERIAL]"
    docs:
      - "docs/debugging-a-boot-loop.md"
      - "docs/porting-a-branch-bump.md"
  - name: "publish-kernel-source.sh"
    path: "tools/publish-kernel-source.sh"
    summary: "publish the kernel source that actually built this ROM, as a normal kernel repo, so a GPL request (or an XDA moderator) can be answered with one link"
    usage: "publish-kernel-source.sh"
    docs: []
  - name: "refresh-patches.sh"
    path: "tools/refresh-patches.sh"
    summary: "re-export overlay/patches/ from the local commits sitting on top of upstream in each patched project"
    usage: "refresh-patches.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "release.sh"
    path: "tools/release.sh"
    summary: "publish the redistributable preset of a build, and refuse to publish anything else"
    usage: "release.sh"
    docs:
      - "docs/OEM-ASSETS.md"
      - "docs/RELEASING.md"
  - name: "repack-erofs-apex.sh"
    path: "tools/repack-erofs-apex.sh"
    summary: "rebuild an APEX whose payload is EROFS so the payload is ext4 instead"
    usage: "repack-erofs-apex.sh <dir-to-scan> [--aosp /aosp] [--keys DIR] [--dry-run]"
    docs: []
  - name: "run-one.sh"
    path: "tools/run-one.sh"
    summary: "build exactly one device, and refuse if anything else is already building"
    usage: "run-one.sh"
    docs: []
  - name: "slot-switch.sh"
    path: "tools/slot-switch.sh"
    summary: "switch an A/B device between the ROMs parked on its two slots, wiping the shared /data so the older one can boot, and putting the fresh install straight on the launcher"
    usage: "slot-switch.sh <a|b> [--keep-data] [--no-provision] [-s SERIAL]"
    docs: []
  - name: "super-loop-mount.sh"
    path: "tools/super-loop-mount.sh"
    summary: "mount a dynamic (logical) partition from recovery without device-mapper"
    usage: "super-loop-mount.sh --list                       partitions, extents, byte offsets"
    docs: []
  - name: "symbolize-odex-pcs.py"
    path: "tools/symbolize-odex-pcs.py"
    summary: "Turn the bare \"pc 0000000002430 70c  /system/framework/oat/arm64/services.odex\" frames of an ANR/tombstone native dump into Java method names"
    usage: "symbolize-odex-pcs.py"
    docs: []
  - name: "sync-forge.sh"
    path: "tools/sync-forge.sh"
    summary: "vendor forge/ into a device repo from rom-forge at a pinned ref"
    usage: "sync-forge.sh"
    docs: []
  - name: "triage-build-log.sh"
    path: "tools/triage-build-log.sh"
    summary: "collapse a `mka -k` build log into distinct failure CLASSES"
    usage: "triage-build-log.sh"
    docs:
      - "docs/porting-a-branch-bump.md"
  - name: "unpack-block-ota.sh"
    path: "tools/unpack-block-ota.sh"
    summary: "turn a block-based OTA zip into a mountable image you can read"
    usage: "unpack-block-ota.sh"
    docs:
      - "docs/OEM-ASSETS.md"
  - name: "usb-watch.sh"
    path: "tools/usb-watch.sh"
    summary: "one line per USB/adb/fastboot state change, timestamped"
    usage: "usb-watch.sh [seconds] [logfile]        default 300 s; Ctrl-C to stop early"
    docs: []
