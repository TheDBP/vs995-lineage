# option.conf -- one build option, carried by the forge, usable on any device.
#
# The switch name is derived, never declared: nav-icons -> WITH_NAV_ICONS. There is no second
# place to get it wrong.
NAME=nav-icons
DESC="Nextbit Robin style nav-bar icons, drawn as scalable tintable vectors"
