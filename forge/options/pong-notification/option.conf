# option.conf -- see forge/options/README.md
NAME=pong-notification
DESC="Pong as the default notification sound (LineageOS default is Argon)"
# Patches vendor/lineage directly, so it is limited to branches with a verified patch.
# lineage-21.0 has no synced tree here to generate one from.
COMPAT=branch=lineage-20.0,branch=lineage-22.2,branch=lineage-24.0
